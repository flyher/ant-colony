// import Vue from 'vue';
import R1 from '../../../src/page/r1.vue';

describe('test r1.vue', () => {
  it('has saveToGlobalVuex function', () => {
    expect(typeof R1.saveToGlobalVuex).toBe('function');
  });
  // const Constructor = Vue.extend(R1);
});
